import { ProductquantityDirective } from './productquantity.directive';

describe('ProductquantityDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductquantityDirective();
    expect(directive).toBeTruthy();
  });
});
