import { Recherche1Pipe } from './recherche1.pipe';

describe('Recherche1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Recherche1Pipe();
    expect(pipe).toBeTruthy();
  });
});
